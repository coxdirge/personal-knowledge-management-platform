# Password for the PostgreSQL container.
# Consumed by both the `postgres` and `backend` services in compose.yaml.
#
# For local development:
#   cp .env.example .env
#
# Never commit the resulting .env — it is gitignored.

POSTGRES_PASSWORD=pkmp
