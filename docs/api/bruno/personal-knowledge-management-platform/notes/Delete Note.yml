info:
  name: Delete Note
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/notes/{{noteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
