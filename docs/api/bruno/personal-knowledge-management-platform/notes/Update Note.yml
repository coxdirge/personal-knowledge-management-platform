info:
  name: Update Note
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/notes/6"
  body:
    type: json
    data: |-
      {
        "title":"csapp",
        "Content":"lead to cs"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
