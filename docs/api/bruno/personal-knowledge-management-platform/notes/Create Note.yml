info:
  name: Create Note
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/notes"
  body:
    type: json
    data: |-
      {
        "title": "置身事内",
        "content": "中国房产制度"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
