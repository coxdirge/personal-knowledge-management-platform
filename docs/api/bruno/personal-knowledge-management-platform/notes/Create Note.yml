info:
  name: Create Note
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/notes"
  body:
    type: json
    data: |-
      {
        "title": "Bruno Test",
        "content": "Created from Bruno"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
