// Shared boundaries keep the refracted English ring and Chinese core separate.
export const LENS_SIZE = 260
export const RIM_SIZE = 22
export const LENS_RADIUS = LENS_SIZE / 2
export const CORE_RADIUS = LENS_RADIUS - RIM_SIZE
